#include "gtest/gtest.h"
#include "linked_cyclic_list/my_linked_cyclic_list.h"

TEST(blaTest, test1) {
    //arrange
    //act
    //assert
    EXPECT_EQ (0, 0);
    EXPECT_EQ (20, 20);
    EXPECT_EQ (100, 100);
}