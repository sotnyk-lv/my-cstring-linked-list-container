#include "gtest/gtest.h"
#include "linked_cyclic_list/my_linked_cyclic_list.h"

TEST(list_creation, test_creation) {
    //arrange
    //act
    //assert
    char **list = nullptr;
    StringListInit(&list);
    list

    EXPECT_EQ (0, 0);
    EXPECT_EQ (20, 20);
    EXPECT_EQ (100, 100);
}